<%= form_for @event do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :date %>
	<%= f.date_select :date, start_year: 2014, end_year: (Time.now.year + 5) %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit %>
<% end %>