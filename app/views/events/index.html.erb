<% @events.each do |event| %>

<h1><%= event.name %></h1>

<h3><%= event.date %></h3>

<h3><%= event.description %></h3>

<button><%=link_to('Edit Event', edit_event_path(event)) %></button>

<% end %>